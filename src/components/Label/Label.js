import React from 'react';

const label = (props) => (
    <div style={{ borderRadius: '3px', padding: '2px 4px', margin: '2px 3px 0 0', fontSize: '.9em', color: 'white', fontFamily: 'Arial', backgroundColor: '#400479bf' }}>
        {props.title}
    </div>
)

export default label;